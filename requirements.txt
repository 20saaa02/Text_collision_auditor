# Основной фреймворк для глубокого обучения
torch

# Библиотека от Hugging Face для работы с трансформерами
transformers

# Для эффективной загрузки больших моделей и работы с квантизацией
accelerate
bitsandbytes

# Библиотека для создания эмбеддингов и реранкинга
sentence-transformers

# Высокопроизводительная библиотека для векторного поиска от Facebook
# Установите ТОЛЬКО ОДНУ из следующих двух строк:
faiss-cpu  # Для работы на центральном процессоре (CPU)
# faiss-gpu  # Для работы на видеокарте NVIDIA (GPU), если она есть

# --------------------------------------------------------------------------
# TEXT PROCESSING & DATA LOADING: Обработка текста и загрузка данных
# --------------------------------------------------------------------------
# Продвинутая библиотека для NLP: токенизация, NER, лемматизация
spacy

# Классическая библиотека для NLP, полезна для некоторых задач (например, sent_tokenize)
nltk

# Для парсинга PDF-файлов
pypdf

# Для парсинга файлов Microsoft Word (.docx)
python-docx

# Для очистки HTML-разметки из текста, если данные из веба
beautifulsoup4

# --------------------------------------------------------------------------
# UTILITIES & API: Вспомогательные инструменты и создание API
# --------------------------------------------------------------------------
# Для валидации данных и определения строгих JSON-схем
pydantic

# Для принудительной генерации структурированного вывода (JSON) из LLM
outlines

# Для создания API, чтобы к вашему приложению могли обращаться другие сервисы
fastapi

# ASGI-сервер для запуска FastAPI
uvicorn

# Для красивых и удобных прогресс-баров в консоли
tqdm

# Для загрузки переменных окружения из .env файла (например, API ключей)
python-dotenv

# Фреймворк для написания и запуска тестов
pytest

# Автоматический форматер кода для поддержания единого стиля
black

# Очень быстрый линтер для поиска ошибок и стилистических проблем
ruff

# Для автоматического запуска проверок (линтеров, тестов) перед коммитом
pre-commit

# Фундаментальная библиотека для научных вычислений
numpy

# Мощная библиотека для манипуляции и анализа данных (таблицы, DataFrame)
pandas

# Основная библиотека для создания графиков и визуализаций
matplotlib

# Высокоуровневая библиотека для создания красивых статистических графиков
seaborn

ipykernel
notebook
jupyter
